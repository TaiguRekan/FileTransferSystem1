
plugins {
    id 'java'
}

group 'FileTransferSystem'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
} // Закрывающая скобка для блока repositories

dependencies {
    implementation 'org.bouncycastle:bcprov-jdk15on:1.70'
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
